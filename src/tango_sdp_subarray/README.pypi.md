# SDPSubarray Tango device server

Python package providing a prototype SKA SDP Subarray device server.

Install with:

```bash
pip install ska-sdp-subarray
```

Usage:

```bash
SDPSubarray <instance name> [-v4]
```
