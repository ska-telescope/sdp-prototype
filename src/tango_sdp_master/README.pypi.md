# SDPMaster Tango device server

Python package providing a prototype SKA SDP Master device server.

Install with:

```bash
pip install ska-sdp-master
```

Usage:

```bash
SDPMaster <instance name> [-v4]
```
